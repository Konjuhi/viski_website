name: swift_shop
description: "SwiftShop web app built with Jaspr."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.4.0

dependencies:
  jaspr: ^0.22.0
  supabase: ^2.0.0

dev_dependencies:
  jaspr_lints: ^0.7.0
  build_runner: ^2.4.0
  jaspr_builder: ^0.22.0
  test: ^1.25.0

jaspr:
  mode: client
